/** Badge marking the team that owed a lap. */
export default function NakedLap() {
  return (
    <span className="rounded-full bg-[var(--color-accent-soft)] px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-[var(--color-accent-dark)]">
      naked lap
    </span>
  );
}
