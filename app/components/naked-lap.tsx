/** Badge marking the team that owed a lap. */
export default function NakedLap() {
  return (
    <span className="rounded-full bg-[#3a2f0c] px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-[var(--color-warn)]">
      naked lap
    </span>
  );
}
